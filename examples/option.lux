// Test enum/ADT codegen
mod option

enum Option<T> {
    Some(T),
    None,
}

fn unwrap_or(opt: Option<Int>, default: Int) -> Int {
    match opt {
        Option::Some(x) => x,
        Option::None => default,
    }
}

fn main() -> Int {
    let some_val = Option::Some(42)
    let none_val = Option::None
    let a = unwrap_or(some_val, 0)
    let b = unwrap_or(none_val, 10)
    a + b
}
